// /command-center — canonical personal dashboard route.
// Re-exports /dashboard/page so both URLs render the same content during
// the IA transition.

export { default } from "@/app/dashboard/page";
