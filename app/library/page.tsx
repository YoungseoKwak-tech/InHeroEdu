// /library — canonical resource hub route.
// Re-exports /textbooks/page so both URLs render the same content during
// the IA transition. Weekly drops feed comes in a later phase.

export { default } from "@/app/textbooks/page";
